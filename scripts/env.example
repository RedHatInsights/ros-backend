# Copy to scripts/.env (gitignored) or export these in your shell.
# Do not commit real values. Docker Compose reads them from scripts/.env.
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
ROS_PROCESSOR_PORT=8004
